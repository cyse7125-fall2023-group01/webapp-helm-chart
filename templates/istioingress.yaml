apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: webapp
  namespace: webapp
spec:
  hosts:
    - "*"
  gateways:
    - webapp-gateway
  http:
    - route:
        - destination:
            host: webapp-service
            port:
              number: 80
