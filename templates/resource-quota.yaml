# apiVersion: v1
# kind: ResourceQuota
# metadata:
#   name: {{ .Release.Name }}-resource-quota
#   namespace: {{ .Values.namespace }}
# spec:
#   hard:
#     requests.memory: "{{ .Values.memoryRequests }}"
#     limits.memory: "{{ .Values.memoryLimits }}"
#     requests.cpu: "{{ .Values.cpuRequests }}"
#     limits.cpu: "{{ .Values.cpuLimits }}"
